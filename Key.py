class Key:
# initialize
  # index number
  # two sets of x,y coordinates

# isUsed() - if used on the correct lock (matches index number?),
  # should remove the key from the game, unlock the lock, and return true. else everything stays the same
